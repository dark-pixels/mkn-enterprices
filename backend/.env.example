# Backend example environment file
# URL of the frontend allowed by CORS (set this to your Vercel frontend)
FRONTEND_URL="https://mkn-enterprices.vercel.app"

# Public backend URL (optional, useful for emails or admin links)
BACKEND_PUBLIC_URL="https://mkn-enterprices-bk.vercel.app"

# Database connection URL - provide your production DB URI here
# Example: mysql://user:password@host:port/database?ssl-mode=REQUIRED
DATABASE_URL="mysql://user:password@host:port/defaultdb?ssl-mode=REQUIRED"

# Admin credentials (override in production via secure secrets)
# ADMIN_USER=admin
# ADMIN_PASS=mknstore

# Port to run the backend (optional)
# PORT=3001

# Copy this file to .env locally and edit values (do NOT commit real .env)
